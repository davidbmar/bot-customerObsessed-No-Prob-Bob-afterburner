# Session

Session-ID: S-2026-03-19-2350-memory-cli
Title: Implement conversation memory system and CLI commands
Date: 2026-03-19
Author: Agent C

## Goal

Build the conversation memory system (JSONL-backed) and CLI commands (start, chat, status) for the afterburner bot.

## Context

Sprint 1, Agent C task. Project scaffolding exists with stub modules. Need to implement ConversationMemory class, rewrite CLI, and add centralized config with data_dir.

## Plan

1. Add data_dir to BotConfig
2. Rewrite memory.py with ConversationMemory (JSONL per conversation_id)
3. Update gateway.py and server.py for new memory API
4. Rewrite cli.py with start/chat/status commands
5. Write and pass tests

## Changes Made

- bot/config.py: Added data_dir field, conversations_dir property, DEFAULT_DATA_DIR constant
- bot/memory.py: Replaced ChatMemory with ConversationMemory class (JSONL-backed, one file per conversation_id)
- bot/gateway.py: Updated to use ConversationMemory instead of per-chat ChatMemory instances
- bot/server.py: Updated /api/history endpoint for new memory API
- cli.py: Rewritten with start/chat/status commands; status shows server reachability, personality, model, conversation count
- tests/test_memory.py: 14 tests covering add/retrieve, isolation, list, clear, JSONL format

## Decisions Made

- ConversationMemory is a single-instance class managing all conversations (vs. per-chat instances) — simpler Gateway code
- Storage at ~/.local/share/afterburner-bot/conversations/ (XDG data dir, not config dir)
- Status command uses httpx GET to check server reachability rather than importing Gateway

## Open Questions

- None

## Links

Commits:
- (see git log after commit)
